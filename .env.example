# =============================================================================
# Frontend Environment Variables (VITE_*)
# =============================================================================
# These variables are exposed to the frontend and included in the bundle
# ONLY include non-sensitive, public configuration here
# =============================================================================

# Application Configuration
VITE_APP_URL=http://localhost:5173
VITE_API_URL=/api

# =============================================================================
# Feature Flags
# =============================================================================
VITE_ENABLE_REACT_QUERY_DEVTOOLS=true
VITE_ENABLE_ERROR_LOGGING=false

# =============================================================================
# SECURITY NOTES
# =============================================================================
# ✅ All API keys are now stored server-side in backend API
# ✅ Frontend only contains public configuration
# ✅ See .env.example.backend for backend environment variables
# ✅ Configure backend variables in Vercel project settings
# ❌ NEVER add VITE_*_API_KEY variables here (they will be in the bundle!)
# ❌ NEVER commit .env.local to version control